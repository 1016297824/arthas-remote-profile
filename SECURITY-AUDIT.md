# 🔍 安全审计报告

> **发布说明**
> - 主副本：本仓库（可推送到 git 的源）
> - 运行位：宿主技能根下的同名目录（把本仓库整个目录拷进去即可）
> - 是否推送由维护者手动决定。
> - 本技能**只含通用方法论**，不含任何实战案例、内网环境信息与业务标识。
>   全目录无凭据值、无真实主机名/IP、无本地部署路径、无专有类名。

## 📊 执行摘要

- **审计对象**: `arthas-remote-profile`
- **审计时间**: 2026-09-17 18:20（18:35 更新：移除实战案例后的复核）
- **审计方式**: 纯静态文本分析（仅使用只读工具：读取文件 / 关键词搜索）
- **审计文件**: `SKILL.md`、`scripts/arthas_profiler.py`、`references/arthas-commands.md`
- **发现问题总数**: 2 个
  - 🔴 P0 阻断级: **0 个**
  - ⚠️ P1 需关注: **2 个**
  - 📝 信息性提醒: **2 个**（非风险项，不计入风险总数）
- **安全评分**: **92 / 100**

---

## 🔴 P0 阻断级风险发现

✅ **未发现 P0 风险**

逐条核对 P0 判定链（Step 0 → A → B → C）：

| P0 条目 | 是否命中 | 说明 |
|---|---|---|
| 下载 + 执行（`curl \| bash`） | ❌ | 无任何远程脚本下载与执行。唯一的 `base64 -d` 是对**本地生成**的辅助脚本解码，非远程内容 |
| 读取敏感信息 + 危险操作 | ❌ | 不读取 `~/.ssh`、`.env` 等；凭据仅从环境变量/`--env-file` 读取 |
| 自动执行破坏性命令 | ❌ | 无 `rm -rf`、无系统配置修改 |
| 隐蔽 + 危险操作 | ❌ | 无 `2>/dev/null` 掩藏危险操作 |
| 权限提升 + 危险操作 | ❌ | 无 `sudo`；仅有对自建 `/tmp/arthas_call.sh` 的 `chmod +x` |

---

## ⚠️ P1 需关注风险发现

### 1. 提供「远程命令执行」能力（能力型，非自动执行）

- **位置**: `scripts/arthas_profiler.py` — `sh` 子命令、`sample --trigger`
- **代码片段**:
  ```python
  _in, out, err = client.exec_command(cmd, timeout=timeout)
  ```
  ```python
  p.add_argument("--trigger", required=True, help="触发一次业务的 shell 命令")
  ```
- **风险描述**: 脚本提供在目标机上执行任意 shell 命令的能力。`sample --trigger` 会在目标机执行调用方
  传入的命令串。
- **判定依据（Step A）**: 该命令**不是硬编码的载荷**，必须由调用方在运行时显式提供，且需先配置 SSH 凭据。
  属于「定义/提供能力」，不属于「自动执行」。按审计规范**最高只能定级 P1**。
- **攻击场景**: 若调用方被诱导传入恶意 `--trigger`，会在目标机执行该命令。风险来自调用方输入，而非技能自身。
- **修复建议**: 使用者只传入「触发被测业务」的只读/幂等请求；不要用它执行写数据、改配置、重启进程的命令。
  （`SKILL.md` 的安全章节已有此约束。）

### 2. `attach` 会在目标机常驻启动一个进程

- **位置**: `scripts/arthas_profiler.py` — `cmd_attach`
- **代码片段**:
  ```python
  run(client, "cd /tmp && nohup java -jar %s %s > /tmp/arthas-boot.log 2>&1 &" % (jar, pid))
  ```
- **风险描述**: 以 `nohup ... &` 方式后台常驻启动 arthas，会在目标机留下一个持续运行的 Java 进程，
  并监听 3658/8563 端口，同时写入 `/tmp/arthas-boot.log`。
- **判定依据（Step B）**: 这不是危险操作 —— arthas 附着是官方标准用法，不修改业务进程、不写业务数据。
- **修复建议**: 诊断结束后在 arthas 里执行 `stop` 退出；`SKILL.md` 已提示采样会在 `/tmp` 留下文件、可清理。

---

## 📝 信息性提醒（非风险项）

1. **base64 传输命令，形态上类似混淆**（信息性提醒）
   - **位置**: `scripts/arthas_profiler.py` — `art_raw()` / `cmd_sample()`
   - **代码片段**: `b64 = base64.b64encode(command.encode()).decode()`
   - **说明**: 目的是规避「本地 shell → SSH → 容器 shell → arthas」四层引号转义，
     被编码的内容是**调用方提供的命令本体**，不存在隐藏载荷。属于常规工程手段。
   - **建议**: 无需修改；`references/arthas-commands.md` 已解释该做法的原因。

2. **docstring 中的密码占位符**（信息性提醒）
   - **位置**: `scripts/arthas_profiler.py` 第 21 行
   - **代码片段**: `export ARTHAS_SSH_PASSWORD='***'`
   - **说明**: 为文档示例占位符，非真实凭据。
   - **建议**: 保持现状即可。

> **已闭环（2026-09-17 18:35）**：原先第 2 项「案例文档含环境信息」随实战案例的移除而消失 ——
> `references/case-cms-publish.md` 已从本技能移出（改由交付目录自留），技能内**不再包含任何实战案例**。

---

## 📋 详细检查结果

### 命令执行与权限检查

- 发现次数: 4 处（均为技能功能本体，非隐藏执行）
- 详细列表:
  - `arthas_profiler.py`：`client.exec_command(...)` —— 通过 SSH 执行命令，技能核心功能
  - `arthas_profiler.py`：`nohup java -jar arthas-boot.jar` —— attach 功能
  - `arthas_profiler.py`：`chmod +x /tmp/arthas_call.sh` —— 对自建临时文件授权
  - `arthas_profiler.py`：无 `subprocess` / `os.system` / `eval` / `exec(` 调用
- **未发现** `curl | bash`、`wget | sh`、`eval(...)`、动态代码加载。

### 文件操作与敏感路径检查

- 发现次数: 2 处
- 详细列表:
  - 写入 `/tmp/` 下临时文件（`arthas_call.sh`、`arthas_cmd.b64`、`arthas_req.json`）—— 合理
  - `sftp.get()` 仅用于把采样结果**从目标机下载到本地**，方向单一
- **未读取**：`~/.ssh`、`~/.aws`、`~/.kube`、`/etc/passwd`、凭证文件
- **无删除操作**：脚本内不存在 `rm` / `unlink` / `shutil.rmtree`

### 网络请求检查

- 发现的 URL（去重）:
  - `http://127.0.0.1:8563`（arthas HTTP API，目标机回环）
  - `http://127.0.0.1:8080`（文档中的示例触发地址，已脱敏为通用端口）
- **无任何外部域名**，**无数据外送**（无 `curl -d` 向第三方发送、无 `nc`、无 webhook）
- Base64 编码检测: 有，但为运行时传输用户命令，非硬编码载荷（见信息性提醒 1）

### 远程脚本深度分析

✅ **不适用** —— 本技能不存在「自动下载并执行远程脚本」的行为，无需触发步骤 4。

### 依赖安装风险检查

- **全局安装检测**: ❌ 未发现。脚本**不自动安装任何依赖**；缺 `paramiko` 时仅报错退出：
  `sys.exit("ERROR: 需要 paramiko（pip install paramiko）")`
- **虚拟环境检查**: ✅ 已在 `SKILL.md` 中补充隔离安装说明（`python -m venv .venv` + 激活后 `pip install paramiko`）
- **依赖来源检查**: ✅ 仅依赖 PyPI 官方源的 `paramiko`，无镜像源指定、无 `git+https://` 直接安装

---

## 💡 总体建议

1. 本技能定位是**只读诊断工具**，与描述一致，无隐藏行为。可以放心安装使用。
2. 使用时的唯一硬约束：`--trigger` 与 `sh` 子命令只传入**触发业务/查看状态**的只读命令，
   不要用它去执行写数据、改配置、重启进程。
3. 凭据管理：通过环境变量或 `--env-file` 传入，用完即删；密码若在对话/截图/日志中出现过，请更换。
4. 目标机上会留下 arthas 进程与 `/tmp` 采样文件，诊断结束后执行 `stop` 并清理临时文件。

---

## ✅ 审计结论

**风险等级**: **P2 — 安全（无投毒风险）**

**使用建议**: ✅ **可以安全使用**

- 未发现任何 P0 阻断级问题：无远程下载执行、无敏感信息窃取、无破坏性操作、无数据外送。
- 2 项 P1 均为「能力型提醒」（提供命令执行能力、会常驻启动 arthas 进程），
  是这类远程诊断工具的必要功能，且都需使用者显式触发，与技能描述完全一致。
- 3 项信息性提醒均不构成投毒风险。

---

**📌 审计原则提醒**
- ✅ 仅报告技能本身的供应链投毒风险
- ❌ 不报告教学代码质量问题
- 🎯 关键判断：技能自动执行了什么操作组合？是否构成危险行为？
